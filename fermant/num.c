#include <stdlib.h>

#include "num.h"

extern inline int icomb(int x, int y);
extern inline uint _ugcd(uint x, uint y);
extern inline uint ugcd(uint x, uint y);
extern inline int igcd(int x, int y);

